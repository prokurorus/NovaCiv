# NovaCiv Ops Agent - Итоговая сводка

## ✅ Что сделано

1. **Создан ops-agent.js** (`server/ops-agent.js`)
   - GitHub API интеграция
   - Безопасный whitelist команд
   - Автоматическое определение owner/repo из git remote
   - Маскировка секретов в выводе
   - Обработка Issues с меткой "ops"

2. **Создан скрипт запуска** (`scripts/setup-ops-agent.sh`)
   - Автоматическая проверка зависимостей
   - Настройка git (если нужно)
   - Запуск через PM2

3. **Создан скрипт настройки git** (`scripts/setup-git.sh`)
   - Проверка/настройка git remote
   - Настройка user.name и user.email

4. **Создана документация**
   - `OPS_AGENT_QUICK_START.md` - быстрый старт (5 строк для GitHub Token)
   - `OPS_AGENT_SETUP.md` - полная инструкция

## 📋 Что нужно сделать

### 1. GitHub Token (5 строк)

```
1. Перейди на https://github.com/settings/tokens
2. Generate new token (classic) → выбери scope `repo`
3. Скопируй токен (формат `ghp_xxxxx`)
4. Добавь в `/root/NovaCiv/.env`: `GITHUB_TOKEN=ghp_xxxxx`
5. Готово
```

### 2. Запуск агента

```bash
cd /root/NovaCiv
bash scripts/setup-ops-agent.sh
```

Или вручную:

```bash
cd /root/NovaCiv
ENV_PATH=/root/NovaCiv/.env pm2 start server/ops-agent.js --name nova-ops-agent --update-env
pm2 save
```

### 3. Проверка работы

```bash
pm2 logs nova-ops-agent
```

## 📝 Доступные команды

- `report:status` - Показать статус системы (PM2, git, disk)
- `video:validate` - Валидировать конфигурацию видео-пайплайна
- `youtube:refresh-test` - Проверить обновление YouTube токена
- `worker:restart` - Перезапустить PM2 worker
- `pipeline:run-test-job` - Создать тестовую задачу для пайплайна

## 🔒 Безопасность

- ✅ Агент выполняет ТОЛЬКО команды из whitelist
- ✅ Никаких произвольных bash команд
- ✅ Секреты автоматически маскируются в выводе
- ✅ Все изменения через git (не через прямые правки файлов)

## 📦 Файлы

```
server/
  └── ops-agent.js          # Главный файл агента

scripts/
  ├── setup-ops-agent.sh    # Скрипт запуска агента
  └── setup-git.sh          # Скрипт настройки git

docs/
  ├── OPS_AGENT_QUICK_START.md   # Быстрый старт (5 строк)
  └── OPS_AGENT_SETUP.md         # Полная инструкция
```

## 🚀 Использование

1. Создай Issue в GitHub репозитории
2. Добавь метку `ops`
3. В title или body укажи команду (например: `report:status`)
4. Агент автоматически обработает Issue и оставит комментарий с результатами

## ⚙️ Переменные окружения

```bash
GITHUB_TOKEN=ghp_xxxxx          # Обязательно
GITHUB_OWNER=NovaCiv            # Опционально (автоопределение из git remote)
GITHUB_REPO=NovaCiv             # Опционально (автоопределение из git remote)
PROJECT_DIR=/root/NovaCiv       # Опционально (по умолчанию /root/NovaCiv)
ENV_PATH=/root/NovaCiv/.env     # Опционально
```

## 🔧 PM2 команды

```bash
pm2 logs nova-ops-agent        # Просмотр логов
pm2 restart nova-ops-agent     # Перезапуск
pm2 stop nova-ops-agent        # Остановка
pm2 status nova-ops-agent      # Статус
pm2 delete nova-ops-agent      # Удаление
```

## 📌 Следующие шаги

1. **Создать Pull Request** с изменениями
2. **Запустить агента** на сервере: `bash scripts/setup-ops-agent.sh`
3. **Создать тестовый Issue** в GitHub с меткой `ops` и командой `report:status`
4. **Проверить работу** агента через комментарии в Issue

## ⚠️ Важные заметки

- Агент автоматически определяет owner/repo из `git remote get-url origin`
- Если git remote не настроен, используется `GITHUB_OWNER` и `GITHUB_REPO` из .env
- Агент обрабатывает каждое Issue только один раз (кэш в памяти)
- Интервал проверки: 60 секунд
- Все секреты автоматически маскируются в выводе (`***REDACTED***`)

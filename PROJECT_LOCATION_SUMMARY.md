# NovaCiv Project Location Summary

## 📍 Основное расположение проекта

**Рабочая директория:** `C:\NovaCiv\NovaCiv\`

---

## 📁 Структура проекта

### 1. **NovaCiv** (корень проекта)
```
C:\NovaCiv\NovaCiv\
├── src/                    # Frontend (React + Vite + TypeScript)
├── netlify/                # Netlify Functions (serverless)
├── server/                 # Отдельный серверный воркер (НЕ для Netlify)
├── media/                  # Медиа-ресурсы для видео-генерации
├── public/                 # Статические файлы
└── package.json
```

---

## 🎬 MEDIA папка

**Путь:** `C:\NovaCiv\NovaCiv\media\`

### Структура:
```
media/
├── backgrounds/           # Фоновые изображения для видео
│   ├── de/               # Немецкий язык
│   │   ├── bg-de-1.jpg
│   │   └── bg-de-2.jpg
│   ├── en/               # Английский язык
│   │   ├── bg-en-1.jpg
│   │   └── bg-en-2.jpg
│   ├── es/               # Испанский язык
│   │   ├── bg-es-1.jpg
│   │   └── bg-es-2.jpg
│   └── ru/               # Русский язык
│       ├── bg-ru-1.jpg
│       └── bg-ru-2.jpg
│
├── scripts/              # Скрипты для обработки видео
│   ├── pipeline.js       # ⭐ ОСНОВНОЙ конвейер генерации видео (TTS + FFmpeg)
│   └── README.md
│
├── voices/               # Конфигурации голосов OpenAI
│   └── openai/
│       ├── nova_de_calm.json
│       ├── nova_en_inspire.json
│       ├── nova_es_calm.json
│       └── nova_ru_calm.json
│
├── subtitles/            # Текстовые файлы субтитров
│   ├── charter/
│   │   ├── en_block_01.txt
│   │   └── ru_block_01.txt
│   └── manifest/
│       ├── de_block_01.txt
│       ├── en_block_01.txt
│       ├── es_block_01.txt
│       └── ru_block_01.txt
│
├── shorts-presets/       # JSON-пресеты для видео
│   ├── short_auto_citation.json
│   ├── short_citation_de_30.json
│   ├── short_citation_en_30.json
│   ├── short_citation_es_30.json
│   └── short_citation_ru_30.json
│
└── brand/
    └── style.md
```

**Ключевой файл:** `media/scripts/pipeline.js` - основной конвейер генерации видео

---

## ⚙️ WORKER файлы

### A) Netlify Functions Worker (для production на Netlify)

**Путь:** `C:\NovaCiv\NovaCiv\netlify\functions\video-worker.js`

**Назначение:**
- Запланирован в cron: каждые 15 минут (`*/15 * * * *`)
- Обрабатывает задачи из Firebase (`videoJobs`)
- Генерирует видео через `media/scripts/pipeline.js`
- Отправляет в Telegram
- Помечает задачи как `done` или `error`

**Конфигурация в `netlify.toml`:**
```toml
[functions."video-worker"]
  schedule = "*/15 * * * *"
```

**Важно:** 
- Использует переменные окружения Netlify
- Работает в serverless окружении
- Включает `media/**` в `included_files`

---

### B) Server Worker (для отдельного сервера)

**Путь:** `C:\NovaCiv\NovaCiv\server\video-worker.js`

**Назначение:**
- Для запуска на отдельном сервере (локальном или удалённом)
- **НЕ используется в Netlify production**
- Работает в бесконечном цикле (проверяет очередь каждые 15 секунд)
- Использует `dotenv` для переменных окружения (`.env` файл)
- Берёт задачи из Firebase (`videoJobs`)
- Рендерит видео через `media/scripts/pipeline.js`
- Отправляет в Telegram
- Загружает на YouTube (если включено)

**Связанные файлы:**
- `server/youtube.js` - утилита для загрузки на YouTube

---

## 🔍 Поиск .env файлов

**Результат поиска:** `.env` файлы **не найдены** в репозитории

**Где должны быть:**
- Для `server/video-worker.js`: `.env` в корне проекта или в `server/`
- Для Netlify Functions: переменные окружения настраиваются в Netlify Dashboard

---

## 📋 Связанные worker функции в Netlify

Все функции находятся в: `C:\NovaCiv\NovaCiv\netlify\functions\`

### Видео-связанные:
- `video-worker.js` ⭐ (cron: каждые 15 мин)
- `create-video-job.js`
- `generate-video.js`
- `generate-video-background.js`

### Другие функции:
- `ai-domovoy.js` - AI ассистент
- `ai-voice.js` - Генерация голоса
- `news-cron.js` - Публикация новостей (cron: каждый час)
- `fetch-news.js` - Получение новостей (cron: каждые 3 часа)
- `domovoy-auto-post.js` - Авто-посты
- `domovoy-auto-reply.js` - Авто-ответы
- `post-to-telegram.js` - Отправка в Telegram
- `send-email.js` - Отправка email

---

## 🗺️ Схема работы видео-воркера

```
Firebase (videoJobs)
    ↓
[worker] Находит задачу со статусом "pending"
    ↓
media/scripts/pipeline.js
    ├── Выбирает фон из media/backgrounds/{lang}/
    ├── Генерирует TTS через OpenAI (голоса из media/voices/openai/)
    ├── Создаёт субтитры из media/subtitles/
    └── Рендерит видео через FFmpeg
    ↓
Отправка в Telegram
    ↓
Обновление статуса в Firebase: "done" или "error"
```

---

## ⚠️ Важные замечания

1. **Два разных worker'а:**
   - `netlify/functions/video-worker.js` - для Netlify (serverless)
   - `server/video-worker.js` - для отдельного сервера

2. **Media папка:**
   - Включена в Netlify Functions через `included_files = ["media/**"]`
   - Используется обоими worker'ами

3. **Переменные окружения:**
   - Netlify: настраиваются в Netlify Dashboard
   - Server worker: требуют `.env` файл (не найден в репозитории)

---

## 🔧 Команды для Windows (PowerShell)

```powershell
# Перейти в проект
cd C:\NovaCiv\NovaCiv

# Показать структуру media
Get-ChildItem -Recurse media\ | Select-Object FullName

# Показать worker файлы
Get-ChildItem -Recurse -Filter "*worker*"

# Поиск .env файлов
Get-ChildItem -Recurse -Filter ".env*" -Force
```

---

**Дата создания:** $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")




# Firebase Realtime Database — Data Model

**Структура данных Firebase RTDB для NovaCiv**

**ВАЖНО:** Этот документ описывает схему данных без реальных значений (без секретов, без реальных контактов/ID пользователей).

---

## 📋 RTDB Overview (Summary)

### Top-Level Nodes

Все данные хранятся в Firebase Realtime Database. Основные ветки:

1. **`config/features/`** — Feature flags (переключатели функций)
   - **Читает:** `server/video-worker.js` (nova-video), `server/config/feature-flags.js`
   - **Критический путь:** ✅ Да (без feature flags система не может корректно работать)

2. **`forum/topics/`** — Темы форума (новости, посты Домового, пользовательские темы)
   - **Читает:** `netlify/functions/fetch-news.js`, `netlify/functions/news-cron.js`, `netlify/functions/domovoy-auto-post.js`, `netlify/functions/domovoy-auto-reply.js`, Frontend (`src/pages/ForumPage.tsx`)
   - **Пишет:** `netlify/functions/fetch-news.js`, `netlify/functions/domovoy-auto-post.js`, Frontend

3. **`forum/comments/`** — Комментарии к темам
   - **Читает:** `netlify/functions/domovoy-auto-reply.js`, Frontend (`src/pages/TopicPage.tsx`)
   - **Пишет:** `netlify/functions/domovoy-auto-reply.js`, Frontend

4. **`videoJobs/`** — Очередь видео-задач
   - **Читает:** `server/video-worker.js` (nova-video PM2 process)
   - **Пишет:** `server/video-worker.js`, внешние источники (если создают задачи)
   - **Критический путь:** ✅ Да (без videoJobs видео-пайплайн не работает)

5. **`contactRequests/`** — Заявки с формы "Присоединиться"
   - **Читает:** `netlify/functions/send-email.js`
   - **Пишет:** Frontend (`src/pages/Join.tsx`)

6. **`messages/`** — Публичный чат
   - **Читает:** Frontend (`src/hooks/useChat.ts`)
   - **Пишет:** Frontend

7. **`newsMeta/`** — Метаданные обработанных новостей (защита от дублей)
   - **Читает/Пишет:** `netlify/functions/fetch-news.js`

8. **`health/news/`** — Heartbeat метрики новостного pipeline
   - **Пишет:** `netlify/functions/fetch-news.js`, `netlify/functions/news-cron.js`
   - **Читает:** `netlify/functions/health-news.js`, `scripts/check-health-news.mjs`
   - **Критический путь:** ✅ Да (для мониторинга работоспособности)

9. **`health/domovoy/`** — Heartbeat метрики Домового pipeline
   - **Пишет:** `netlify/functions/domovoy-auto-post.js`, `netlify/functions/domovoy-auto-reply.js`
   - **Читает:** `netlify/functions/health-domovoy.js`, `scripts/check-health-domovoy.mjs`
   - **Критический путь:** ✅ Да (для мониторинга работоспособности)

10. **`assistantMessages/`** — История диалогов с Домовым (чат виджет)
    - **Пишет:** `netlify/functions/ai-domovoy.js`
    - **Читает:** Frontend (`src/components/AssistantWidget.tsx`)

11. **`stats/`** — Статистика сайта (visitors, likes, joined)
    - **Пишет/Читает:** Frontend (`src/pages/Join.tsx`, `src/hooks/useStats.ts`)

### Critical Paths (Критические пути)

**Без этих узлов система считается неработоспособной:**

- ✅ `config/features/` — Feature flags (без них система не может корректно управлять функциями)
- ✅ `videoJobs/` — Видео-пайплайн (без него видео не обрабатываются)
- ✅ `health/news/`, `health/domovoy/` — Мониторинг (без них нельзя проверить работоспособность)

**Важно:** Все critical paths должны быть доступны и корректно работать. Нарушение = инцидент.

---

## 📊 Основные ветки (детально)

### `config/features/`

**Назначение:** Feature flags (переключатели функций)

**Структура:**
```json
{
  "config": {
    "features": {
      "youtubeUploadEnabled": false,
      "telegramEnabled": true
    }
  }
}
```

**Поля:**
- `youtubeUploadEnabled` (boolean) — включение/выключение загрузки видео на YouTube
- `telegramEnabled` (boolean) — включение/выключение отправки в Telegram

**Значения по умолчанию:**
- `youtubeUploadEnabled: false` (безопасный дефолт)
- `telegramEnabled: true`

**Где используется:**
- `server/video-worker.js` — проверяет флаги перед загрузкой на YouTube/Telegram
- `server/config/feature-flags.js` — функция чтения флагов

---

### `forum/topics/`

**Назначение:** Темы форума (новости, посты Домового, пользовательские темы)

**Структура:**
```
forum/topics/{topicId}
```

**Поля:**
- `title` (string) — заголовок темы
- `content` (string) — содержимое темы (текст)
- `section` (string) — раздел форума (например: "news", "general")
- `createdAt` (number) — timestamp создания (milliseconds)
- `createdAtServer` (number) — timestamp создания на сервере (milliseconds)
- `authorNickname` (string) — никнейм автора (например: "NovaCiv News", "Домовой NovaCiv")
- `lang` (string) — язык темы ("ru", "en", "de")
- `postKind` (string) — тип поста (например: "domovoy:charter_quote", "news")
- `sourceId` (string) — идентификатор источника (например: "domovoy", RSS source ID)
- `originalGuid` (string) — оригинальный GUID (для новостей из RSS)
- `originalLink` (string) — оригинальная ссылка (для новостей из RSS)
- `pubDate` (string) — дата публикации (для новостей из RSS)
- `telegramPosted` (object) — флаги отправки в Telegram по языкам:
  ```json
  {
    "ru": true,
    "en": true,
    "de": false
  }
  ```

**Примеры значений `section`:**
- `"news"` — новости и посты Домового
- `"general"` — общие темы (пользовательские)

**Примеры значений `postKind`:**
- `"domovoy:charter_quote"` — пост Домового (цитата из Устава)
- `"domovoy:question_to_reader"` — пост Домового (вопрос читателю)
- `"domovoy:term_explainer"` — пост Домового (объяснение термина)
- `"domovoy:charter_series"` — пост Домового (серия по Уставу)
- `"news"` — новость из RSS

**Где используется:**
- `netlify/functions/fetch-news.js` — создаёт темы для новостей
- `netlify/functions/domovoy-auto-post.js` — создаёт темы для постов Домового
- `netlify/functions/news-cron.js` — читает темы для отправки в Telegram
- `netlify/functions/domovoy-auto-reply.js` — читает темы для ответов
- Frontend (`src/pages/ForumPage.tsx`, `src/pages/TopicPage.tsx`) — отображение тем

---

### `forum/comments/`

**Назначение:** Комментарии к темам форума

**Структура:**
```
forum/comments/{topicId}/{commentId}
```

**Поля:**
- `content` (string) — текст комментария
- `authorNickname` (string) — никнейм автора комментария
- `createdAt` (number) — timestamp создания (milliseconds)
- `lang` (string) — язык комментария ("ru", "en", "de")
- `domovoyReplied` (boolean) — флаг, что Домовой уже ответил на этот комментарий
- `isSystem` (boolean) — флаг системного/ботского комментария
- `sourceId` (string) — идентификатор источника (например: "community")

**Где используется:**
- `netlify/functions/domovoy-auto-reply.js` — читает комментарии, создаёт ответы Домового
- Frontend (`src/pages/TopicPage.tsx`) — отображение комментариев

---

### `videoJobs/`

**Назначение:** Очередь задач генерации видео-шортов

**Структура:**
```
videoJobs/{jobId}
```

**Поля:**
- `status` (string) — статус задачи: `"pending"` | `"processing"` | `"done"` | `"error"`
- `targets` (array) — цели для публикации (например: `["youtube", "telegram"]`)
- `language` (string) — язык видео ("ru", "en", "de", "es")
- `script` (string) — исходный текст ролика
- `title` (string) — заголовок видео
- `topic` (string) — связанная тема (опционально)
- `caption` (string) — подпись для Telegram
- `videoPath` (string) — путь к сгенерированному видео файлу
- `youtubeId` (string) — ID видео на YouTube (после загрузки)
- `youtubeError` (string) — ошибка загрузки на YouTube (если была)
- `lockedAt` (number) — timestamp блокировки задачи (milliseconds)
- `lockedBy` (string) — идентификатор воркера, который обрабатывает задачу
- `createdAt` (number) — timestamp создания задачи (milliseconds)
- `finishedAt` (number) — timestamp завершения задачи (milliseconds)
- `errorMessage` (string) — сообщение об ошибке (если status = "error")

**Где используется:**
- `server/video-worker.js` (PM2) — обрабатывает задачи
- `netlify/functions/video-worker.js` — создаёт задачи (опционально, может быть отключено)

---

### `contactRequests/`

**Назначение:** Заявки с формы "Присоединиться"

**Структура:**
```
contactRequests/{requestId}
```

**Поля:**
- `nickname` (string) — псевдоним пользователя
- `contact` (string) — контактная информация (email/Telegram)
- `message` (string) — сообщение пользователя
- `language` (string) — язык сайта, на котором была отправлена форма
- `memberId` (string) — ID участника
- `source` (string) — источник заявки (например: "join_form")
- `status` (string) — статус обработки (например: "pending", "processed")
- `createdAt` (number) — timestamp создания заявки (milliseconds)

**ВАЖНО:** Не хранить реальные контакты/почты/ID в документации. Только схема полей.

**Где используется:**
- `netlify/functions/send-email.js` — обрабатывает заявки, отправляет email и Telegram уведомления
- Frontend (`src/pages/Join.tsx`) — отправка заявок

---

### `messages/`

**Назначение:** Сообщения публичного чата

**Структура:**
```
messages/{messageId}
```

**Поля:**
- `nickname` (string) — никнейм отправителя
- `text` (string) — текст сообщения (максимум 300 символов)

**Где используется:**
- Frontend (`src/hooks/useChat.ts`) — отправка и получение сообщений
- Frontend (`src/pages/Join.tsx`) — отображение чата

---

### `newsMeta/`

**Назначение:** Метаданные обработанных новостей (защита от дублей)

**Структура:**
```
newsMeta/{lang}.json
```

**Пример для английского языка:**
```json
{
  "newsMeta": {
    "en": {
      "processedKeys": ["guid1", "guid2", "guid3"],
      "titleKeys": ["title1", "title2", "title3"]
    }
  }
}
```

**Поля:**
- `processedKeys` (array) — массив GUID уже обработанных новостей
- `titleKeys` (array) — массив заголовков уже обработанных новостей

**Где используется:**
- `netlify/functions/fetch-news.js` — проверяет дубли перед обработкой, обновляет метаданные

---

### `health/news/`

**Назначение:** Heartbeat метрики для новостного pipeline

**Структура:**
```
health/news/fetchNewsLastRun
health/news/newsCronLastRun
```

**Поля (`fetchNewsLastRun`):**
- `ts` (number) — timestamp последнего запуска (milliseconds)
- `runId` (string) — идентификатор запуска
- `sourcesOk` (number) — количество успешно обработанных RSS источников
- `sourcesFailed` (number) — количество источников с ошибками
- `fetched` (number) — количество скачанных новостей
- `filtered` (number) — количество отфильтрованных новостей
- `processed` (number) — количество обработанных новостей
- `ok` (boolean) — флаг успешности выполнения
- `errCode` (string) — код ошибки (если была)

**Поля (`newsCronLastRun`):**
- `ts` (number) — timestamp последнего запуска (milliseconds)
- `runId` (string) — идентификатор запуска
- `fetchedTopics` (number) — количество прочитанных тем
- `processed` (number) — количество обработанных тем
- `totalSent` (number) — общее количество отправленных сообщений в Telegram
- `perLanguage` (object) — статистика по языкам:
  ```json
  {
    "ru": { "sent": 5, "errors": [] },
    "en": { "sent": 5, "errors": [] },
    "de": { "sent": 3, "errors": [] }
  }
  ```
- `ok` (boolean) — флаг успешности выполнения
- `errCode` (string) — код ошибки (если была)

**Где используется:**
- `netlify/functions/fetch-news.js` — пишет метрики в `fetchNewsLastRun`
- `netlify/functions/news-cron.js` — пишет метрики в `newsCronLastRun`
- `netlify/functions/health-news.js` — читает метрики для health endpoint
- `scripts/check-health-news.mjs` — проверяет метрики для мониторинга

---

### `health/domovoy/`

**Назначение:** Heartbeat метрики для Домового pipeline

**Структура:**
```
health/domovoy/autoPostLastRun
health/domovoy/autoReplyLastRun
```

**Поля (`autoPostLastRun`):**
- `ts` (number) — timestamp последнего запуска (milliseconds)
- `runId` (string) — идентификатор запуска
- `ok` (boolean) — флаг успешности выполнения
- `postedPerLang` (object) — количество созданных постов по языкам:
  ```json
  {
    "ru": 1,
    "en": 1,
    "de": 0
  }
  ```
- `telegramSentPerLang` (object) — количество отправленных в Telegram по языкам:
  ```json
  {
    "ru": 1,
    "en": 1,
    "de": 0
  }
  ```
- `errCode` (string) — код ошибки (если была)

**Поля (`autoReplyLastRun`):**
- `ts` (number) — timestamp последнего запуска (milliseconds)
- `runId` (string) — идентификатор запуска
- `ok` (boolean) — флаг успешности выполнения
- `scanned` (number) — количество просканированных комментариев
- `repliedPerLang` (object) — количество ответов по языкам:
  ```json
  {
    "ru": 2,
    "en": 1,
    "de": 0
  }
  ```
- `errCode` (string) — код ошибки (если была)

**Где используется:**
- `netlify/functions/domovoy-auto-post.js` — пишет метрики в `autoPostLastRun`
- `netlify/functions/domovoy-auto-reply.js` — пишет метрики в `autoReplyLastRun`
- `netlify/functions/health-domovoy.js` — читает метрики для health endpoint
- `scripts/check-health-domovoy.mjs` — проверяет метрики для мониторинга

---

### `assistantMessages/`

**Назначение:** История диалогов с Домовым (чат виджет)

**Структура:**
```
assistantMessages/{sessionId}/{messageId}
```

**Поля:**
- `role` (string) — роль: `"user"` | `"assistant"`
- `text` (string) — текст сообщения
- `language` (string) — язык диалога ("ru", "en", "de", "es")
- `createdAt` (number) — timestamp создания (milliseconds)

**Где используется:**
- `netlify/functions/ai-domovoy.js` — обрабатывает запросы пользователей, создаёт ответы
- Frontend (`src/components/AssistantWidget.tsx`) — отображение чата с Домовым

---

### `stats/`

**Назначение:** Статистика сайта

**Структура:**
```
stats/visitors
stats/likes
stats/joined
```

**Поля:**
- Значения — числа (counters)

**Где используется:**
- Frontend (`src/pages/Join.tsx`) — отображение статистики
- Frontend (`src/hooks/useStats.ts`) — обновление статистики

---

## 🔗 Связи между ветками

- `forum/topics` ↔ `forum/comments` — комментарии связаны с темами через `topicId`
- `videoJobs` → `forum/topics` — видео-задачи могут быть связаны с темами через поле `topic`
- `health/news/*` → `netlify/functions/fetch-news.js`, `netlify/functions/news-cron.js`
- `health/domovoy/*` → `netlify/functions/domovoy-auto-post.js`, `netlify/functions/domovoy-auto-reply.js`

---

## 📋 Резюме

**Основные ветки:**
1. `config/features/` — feature flags
2. `forum/topics/` — темы форума
3. `forum/comments/` — комментарии к темам
4. `videoJobs/` — очередь видео-задач
5. `contactRequests/` — заявки с формы "Присоединиться"
6. `messages/` — публичный чат
7. `newsMeta/` — метаданные новостей
8. `health/news/` — метрики новостного pipeline
9. `health/domovoy/` — метрики Домового pipeline
10. `assistantMessages/` — история диалогов с Домовым
11. `stats/` — статистика сайта

---

*Документ обновляется при изменениях в структуре данных Firebase RTDB.*

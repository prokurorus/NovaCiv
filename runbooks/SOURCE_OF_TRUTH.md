# Source of Truth — NovaCiv

**Жёсткий регламент синхронизации**

---

## 🎯 Правило источника истины

**Source of Truth = GitHub main**

Все изменения кода делаются на ПК → commit/push → GitHub.

**Cursor работает только с локальной копией на ПК или напрямую с GitHub.**

**Cursor НЕ вносит правки кода на сервере в обычном режиме.**

**Сервер = runtime only (выполнение кода, без правок).**

---

## 📋 Правила работы

### ✅ Разрешено на сервере

1. **Переменные окружения:**
   - Редактирование `.env` файла
   - Изменение системных переменных окружения

2. **Системные конфиги:**
   - Настройки PM2 (pm2 ecosystem, pm2 startup)
   - Настройки cron (crontab)
   - Настройки nginx/systemd (если используются)

3. **Инфраструктурные настройки:**
   - Настройки файрвола
   - Настройки SSH
   - Системные обновления

### ❌ Запрещено на сервере

1. **Правки кода:**
   - Изменение файлов в `/root/NovaCiv/` (кроме `.env`)
   - Ручные правки в `netlify/functions/*.js`
   - Ручные правки в `server/*.js`
   - Ручные правки в `scripts/*.mjs`, `scripts/*.sh`

2. **Git операции (кроме pull):**
   - `git commit` (заблокирован pre-commit hook)
   - `git push` (заблокирован)
   - Ручное изменение истории (`git rebase`, `git reset --hard`)

---

## 🔄 Стандартный workflow

### Разработка на ПК

1. Изменить код на ПК (в Cursor/IDE)
2. Закоммитить: `git commit -m "описание изменений"`
3. Запушить: `git push origin main`

### Деплой на сервер

**Использовать runbook:**
```bash
bash /root/NovaCiv/runbooks/deploy_pull_only.sh
```

**Или вручную:**
```bash
cd /root/NovaCiv
git fetch origin
git reset --hard origin/main
pm2 restart all
```

**Через ops-agent:**
- Создать GitHub Issue с меткой "ops"
- Команда: `deploy` (если реализовано)

---

## ⚠️ Архитектурное правило

**Любое состояние `git status != clean` на сервере считается инцидентом архитектуры.**

Это означает нарушение pull-only режима и требует немедленного исправления согласно процедуре в разделе "Dirty Repository — инцидент".

---

## 🚨 Аварийное исключение

**Если правка кода сделана на сервере (крайний случай):**

1. **Немедленно закоммитить и запушить:**
   ```bash
   cd /root/NovaCiv
   git add -A
   git commit -m "hotfix: описание изменений"
   git push origin main
   ```

2. **На ПК сделать pull:**
   ```bash
   git pull origin main
   ```

3. **Задокументировать:**
   - Создать Issue в GitHub с описанием причины экстренной правки
   - Указать, почему нельзя было сделать через обычный workflow

**Цель:** Свести такие случаи к нулю. Аварийные правки на сервере должны быть исключением, а не правилом.

---

## ⚠️ Dirty Repository — инцидент

**Если `git status` показывает "dirty":**

Это считается инцидентом "нарушен pull-only режим".

**Процедура возврата в чистое состояние:**

1. **Проверить изменения:**
   ```bash
   cd /root/NovaCiv
   git status --short
   git diff --stat
   ```

2. **Если изменения нужны:**
   - Закоммитить и запушить (см. "Аварийное исключение")
   - Затем на ПК сделать pull

3. **Если изменения не нужны:**
   ```bash
   cd /root/NovaCiv
   git reset --hard origin/main
   git clean -fd  # Удалить неотслеживаемые файлы (осторожно!)
   ```

4. **Проверить состояние:**
   ```bash
   git status  # Должен показать "clean"
   git log -1  # Проверить последний коммит
   ```

5. **Вернуть snapshot в чистое состояние:**
   ```bash
   bash /root/NovaCiv/runbooks/snapshot_system.sh
   ```

**Важно:** Не использовать `git clean -fd` если есть важные неотслеживаемые файлы (например, временные файлы, логи).

---

## 📊 Мониторинг

**Snapshot автоматически проверяет git статус:**
- Если `git status = dirty` → snapshot показывает "dirty"
- Это красный флаг в мониторинге

**Проверка вручную:**
```bash
cd /root/NovaCiv
git status
```

**Если snapshot показывает "dirty":**
1. Проверить: `git status --short`
2. Следовать процедуре выше ("Dirty Repository — инцидент")

---

## 🔒 Защита от нарушений

**Pre-commit hook на сервере:**
- Блокирует `git commit` на сервере
- Сообщение: "Server is pull-only. Commits are not allowed on the server."

**Git config:**
- `git config user.name` = "SERVER-DO-NOT-COMMIT"
- `git config user.email` = "SERVER-DO-NOT-COMMIT"

---

*Этот регламент обязателен для соблюдения всеми операторами.*
